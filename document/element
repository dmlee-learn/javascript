element is item in document

we can change to the element property like innerText, autofocus, innerHtml 

 accese ID
  - let item = document.getElementById(id);
     ex item.innerText = "right now";
  - document.getElementsByClassName
  - document.getElementsByName
  - document.getElementsByTagName
  - document.getElementsByTagNameNS
  - document.querySelector(".ClassName h1")     //css style find / find first match itme / find class in h1 / noting is tag, . is class, # is id
  - document.querySelectorAll

in property 
 - event
   name is start on~ ex) on mouse, keydown, keyup, click
   have listen
   use : let element = document.querySelector("#ClassName h1")
         let handleClick = () => {
             console.log('element clicked');
         }
         element.addEventListener("click", handleClick);
         element.onclick = handleClick;
 - style 
   property ex element.style.color = 'red'
